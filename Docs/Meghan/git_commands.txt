git branch "<branch_name>"

git checkout -a

git checkout <branch_name>


TO COMMIT ON YOUR BRANCH

"""make your file"""
git checkout <branch_name> //make the new branch

git add "Path/to/file or file_name" 

git commit -m "Pushing file for <branch_name>"

git push

To merge two branches:

git checkout [branchYouWantToReceiveBranch] //checkout branch to receive branch

git merge [branchYouWantToMergeIntoBranch]
